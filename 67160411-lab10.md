# ❓ คำถามวัดความเข้าใจ - Lab10: Movie Database SPA

---

## 📚 เรื่อง 1: Fetch API และ HTTP Requests

### 1. ฟังก์ชัน `fetch()` ใช้ส่ง HTTP request ประเภทไหนเป็นค่าเริ่มต้น

ค่าเริ่มต้นคือ **GET request**  
ถ้าไม่กำหนด method เอง ระบบจะใช้ GET อัตโนมัติ

---

### 2. เหตุใดจึงต้องใช้ `.json()` หลังจาก `fetch()`

เพราะ `fetch()` จะได้ค่าเป็น Response object ยังไม่ใช่ข้อมูลจริง  
ต้องใช้ `.json()` เพื่อแปลงข้อมูล JSON ให้เป็น JavaScript object ก่อนจึงจะนำไปใช้งานได้

---

### 3. API response ของ OMDB มี Error handling อย่างไร (ยกตัวอย่างจากโค้ด)

OMDB จะส่งค่าแบบนี้เมื่อเกิดข้อผิดพลาด

```json
{
  "Response": "False",
  "Error": "Movie not found!"
}
```

```js
if (data.Response === "False") {
  console.error("API Error:", data.Error);
  return [];
}
```

ถ้า Response เป็น False จะถือว่าเกิด error แล้ว return ค่าว่างกลับไป

### 4. ถ้า API key ผิด จะได้ข้อมูลเป็นอะไร และควรตรวจสอบอย่างไร

```json
{
  "Response": "False",
  "Error": "Invalid API key!"
}
```

ควรตรวจสอบโดยเช็คว่า data.Response === "False" แล้วแสดง error ออกมา

---

### 5. ฟังก์ชัน `API.getMovies()` ดึงข้อมูลภาพยนตร์กี่เรื่อง และค้นหาคำว่าอะไร

ฟังก์ชันนี้ค้นหาคำว่า "movie"
และใช้ .slice(0, 5)
ดังนั้นจะดึงมา 5 เรื่องแรก

---

### 6. ความแตกต่างระหว่าง `API.getMovies()` กับ `API.searchMovies()` คืออะไร

- getMovies() ค้นหาคำว่า "movie" และจำกัด 5 เรื่อง

- searchMovies(query) ค้นหาตามคำที่ผู้ใช้พิมพ์ และไม่จำกัดแค่ 5 เรื่อง

---

### 7. ฟังก์ชัน `API.getMovieById()` ใช้พารามิเตอร์อะไรในการค้นหาภาพยนตร์เรื่องเดี่ยว

ใช้ IMDb ID เช่น tt0133093
โดยส่งค่าแบบ ?i=imdbID

---

### 8. ทำไม OMDB search response ไม่มี rating แต่ detail response มี

เพราะ API แยกเป็น 2 แบบ

- ?s= ใช้ค้นหา จะได้ข้อมูลแบบย่อ (ไม่มี rating)

- ?i= ใช้ดูรายละเอียด จะได้ข้อมูลครบ รวมถึง rating

---

### 9. URL ของ OMDB API ในโค้ดชี้ไปยังเซิร์ฟเวอร์ไหน (ให้ดูจาก `BASE_URL`)

ชี้ไปที่ <https://www.omdbapi.com> ซึ่งเป็นเซิร์ฟเวอร์ของ OMDb API

---

### 10. ฟังก์ชัน `API.searchMovies()` จะส่งอะไรกลับมา ถ้า `query` เป็นว่างหรือ null

ถ้า query ว่าง จะส่งกลับมาเป็น array ว่าง ([])

---

## 🎨 เรื่อง 2: SPA และ Hash-based Routing

### 1. SPA ย่อมาจากคำอะไร และมีข้อดีอย่างไรกว่า Traditional Web

SPA ย่อมาจาก **Single Page Application**
ข้อดี

- โหลดหน้าเว็บครั้งเดียว
- เปลี่ยนหน้าโดยไม่ต้อง reload ทั้งเว็บ
- ทำงานเร็วและลื่นกว่า Traditional Web
- ประสบการณ์ใช้งานเหมือนแอปพลิเคชัน

---

### 2. Hash-based routing เมื่อเปลี่ยน URL hash จะทำให้ browser reload หน้าหรือไม่

ไม่ reload หน้าเว็บ  
เพราะการเปลี่ยนแค่ `#` (hash) จะไม่ทำให้ browser โหลดหน้าใหม่  
แต่จะ trigger event `hashchange` แทน

---

### 3. Route `#/movie/:id` ใช้ตัวแปร `:id` ประเภทอะไร (ยกตัวอย่าง imdbID จริง)

ใช้เป็น **String**  
ตัวอย่าง IMDb ID จริง เช่น  
`tt1375666` (Inception)

---

### 4. ฟังก์ชัน `navigate()` จะถูกเรียกใช้ เมื่อเกิดเหตุการณ์ใดสองครั้ง

ถูกเรียกเมื่อเกิด 2 เหตุการณ์คือ

1. เมื่อหน้าเว็บโหลดเสร็จ (`load`)
2. เมื่อ URL hash เปลี่ยน (`hashchange`)

---

### 5. คำสั่ง `hash.slice(2)` ทำไมถึงต้องตัด 2 ตัวอักษร (ตัดอะไรออก)

เพราะ URL เป็นรูปแบบ `#/movies`  
จึงต้องตัด `#/` ออก 2 ตัวอักษร เพื่อให้เหลือแค่ `movies`

---

### 6. ในโค้ด router ถ้า path มีค่าเป็น `#/movie/tt1375666` จะถูก split เป็น parts อะไรบ้าง

หลังจาก slice แล้วจะได้ movie/tt1375666
เมื่อใช้ `split("/")` จะได้ ["movie", "tt1375666"]

---

### 7. โค้ดที่จะแสดงใน `appDiv.innerHTML` มาจาก switch case กี่กรณี และมีกรณีไหนบ้าง

มีทั้งหมด 5 กรณี

1. `home`
2. `movies`
3. `movie`
4. `search`
5. `default` (กรณีไม่พบหน้า)

---

### 8. ฟังก์ชัน `updateNavigation()` ทำหน้าที่อะไร (ให้อธิบายการเปลี่ยนแปลง CSS class)

ทำหน้าที่เปลี่ยน CSS class ของเมนูให้ตรงกับหน้าปัจจุบัน  
โดยจะเพิ่ม class `"active"` ให้กับลิงก์ที่ตรงกับ route และลบ `"active"` ออกจากลิงก์อื่น
ทำให้เมนูที่กำลังใช้งานอยู่มีสีหรือสไตล์แตกต่างจากอันอื่น

---

### 9. ถ้า user ไปที่ route ที่ไม่มีใน switch case จะแสดงอะไร

จะแสดงหน้า 404 - ไม่พบหน้า ซึ่งมาจาก `Views.notFound()`

---

### 10. Event listener `hashchange` จะถูก trigger เมื่อ URL เปลี่ยนเป็น `#/movies` หรือไม่

ถูก trigger  
เพราะมีการเปลี่ยนค่า hash (`#`) จึงทำให้ event `hashchange` ทำงาน

---

## 💾 เรื่อง 3: API Caching และ Performance

### 1. วัตถุประสงค์ของ `API.cache` object คืออะไร

ใช้สำหรับ **เก็บข้อมูลที่เคยดึงจาก API แล้ว**  
เพื่อไม่ต้องเรียก API ซ้ำหลายครั้ง

---

### 2. ฟังก์ชัน `getMovies()` จะตรวจสอบ cache ก่อนหรือหลังจาก fetch

รวจสอบ **ก่อน fetch**

ในโค้ดมี

```js
if (this.cache.movies) {
  return this.cache.movies;
}
```

ถ้ามีข้อมูลใน cache แล้ว จะไม่เรียก API ใหม่

---

### 3. Cache key สำหรับภาพยนตร์ คืออะไร (ดูจาก `this.cache.movies`)

คือ key ชื่อ "movies"

---

### 4. ถ้าภาพยนตร์มีข้อมูลใหม่ออกมา cache เก่าจะลบออกอัตโนมัติหรือไม่

ไม่ลบอัตโนมัติ
เพราะในโค้ดไม่มีระบบลบหรืออัปเดต cache เอง

---

### 5. ประโยชน์ของการใช้ cache คืออะไร (ยกเหตุผล 2 ประการ)

1. ลดจำนวนการเรียก API (ประหยัด request)
2. ทำให้เว็บโหลดเร็วขึ้น

---

### 6. ปัญหาเมื่อใช้ cache คืออะไร และแก้ไขอย่างไร

ปัญหา: ข้อมูลอาจไม่อัปเดต (ข้อมูลเก่า)

วิธีแก้:

- ลบ cache แล้ว fetch ใหม่
- ใช้เวลาหมดอายุ (เช่น ตั้ง timeout)
- มีปุ่ม refresh เพื่อโหลดข้อมูลใหม่

---

### 7. Cache ของ movie detail เก็บด้วย key ไหน (imdbID หรือ title)

imdbID

---

### 8. ถ้า user ค้นหา "Inception" สองครั้ง จะใช้ API ทั้งสองครั้งหรือไม่

จะเรียก API ทั้งสองครั้ง
เพราะ searchMovies() ไม่มีระบบ cache

---

### 9. การรีเซ็ต cache ทำได้อย่างไร (ถ้าต้องการข้อมูลใหม่)

สามารถล้าง cache ได้ เช่น

```js
API.cache = {};
```

แล้วเรียก API ใหม่อีกครั้ง

---

### 10. Cache มีพื้นที่เก็บข้อมูลจำกัดหรือไม่ ควรมี strategy สำหรับการลบหรือไม่

ถ้าเก็บใน memory (แบบนี้) จะขึ้นอยู่กับ browser

ควรมี strategy เช่น

- ลบข้อมูลเก่าเมื่อเกินจำนวนที่กำหนด
- ตั้งเวลาหมดอายุ (TTL)
- ล้าง cache เมื่อ reload หน้าเว็บ
- เพื่อป้องกันใช้ memory มากเกินไป

---

## 🔗 เรื่อง 4: CORS และ Cross-Origin Requests

### 1. CORS ย่อมาจากคำอะไร และเหตุใดจึงมี CORS policy

CORS ย่อมาจาก **Cross-Origin Resource Sharing**

มีขึ้นมาเพื่อความปลอดภัย  
ป้องกันไม่ให้เว็บไซต์หนึ่งดึงข้อมูลจากอีกเว็บไซต์หนึ่งโดยไม่ได้รับอนุญาต

---

### 2. Browser จะบล็อก request ข้ามโดเมน (cross-origin) ด้วยเหตุผลใด

เพื่อป้องกันการโจมตี เช่น

- การขโมยข้อมูลผู้ใช้
- การดึงข้อมูลลับจากเว็บไซต์อื่น

จึงต้องมีการอนุญาตจาก server ก่อน

---

### 3. OMDB API รองรับ CORS หรือไม่ ยกหลักฐานจากการทำงาน

รองรับ

หลักฐานคือ เราสามารถใช้ `fetch()` เรียก API จาก browser ได้โดยไม่เกิด CORS error  
แสดงว่า server ของ OMDB ส่ง header อนุญาต CORS มาแล้ว

---

### 4. ความแตกต่างระหว่าง same-origin และ cross-origin request คืออะไร

- Same-origin = domain, protocol และ port เหมือนกัน
- Cross-origin = ต่างกันอย่างใดอย่างหนึ่ง เช่น เรียกจาก `localhost` ไป `omdbapi.com`

---

### 5. เมื่อเกิด CORS error ข้อความจะเป็นแบบไหน (ยกตัวอย่าง)

ตัวอย่างข้อความ error:
Access to fetch at 'https://api.example.com
from origin 'http://localhost:5500
has been blocked by CORS policy

มักจะแสดงใน Console

---

### 6. Server ต้องส่ง HTTP header ไหนเพื่ออนุญาต CORS request

ต้องส่ง header เช่น
Access-Control-Allow-Origin: http://localhost:5500

---

### 7. `fetch()` API จะส่ง CORS preflight request (OPTIONS) เมื่อไร

จะส่ง preflight (OPTIONS) เมื่อ

- ใช้ method ที่ไม่ใช่ GET/POST ธรรมดา เช่น PUT, DELETE
- มี custom headers
- ส่ง Content-Type ที่ไม่ปกติ เช่น application/json บางกรณี

---

### 8. API key ใน URL ของ OMDB API มีประโยชน์ต่อ CORS policy หรือไม่

ไม่มีผลโดยตรงกับ CORS

API key ใช้สำหรับยืนยันสิทธิ์ (authentication)  
ไม่เกี่ยวกับการอนุญาต cross-origin

---

### 9. ถ้า API ไม่รองรับ CORS จะใช้วิธีแก้ไขไหน (JSONP, Proxy, etc.)

วิธีแก้ เช่น

- ใช้ **Proxy Server** (ให้ server ของเราดึง API แทน)
- ใช้ **Backend** เป็นตัวกลาง
- ใช้ JSONP (วิธีเก่า ไม่ค่อยนิยมแล้ว)

---

### 10. CORS error ส่งผลต่อการแสดงผลหน้าเว็บหรือไม่ (Network tab vs Console)

ส่งผล

- ใน **Console** จะขึ้น CORS error
- ใน **Network tab** จะเห็น request ล้มเหลว
- หน้าเว็บจะไม่แสดงข้อมูล เพราะ fetch ไม่สำเร็จ

---

## 🎯 เรื่อง 5: DOM Manipulation และ Events

### 1. Element `#app` ใช้เก็บอะไร และทำไมถึงเลือก `innerHTML` แทน `textContent`

`#app` ใช้สำหรับเก็บเนื้อหาของแต่ละหน้าใน SPA  
เช่น Home, Movie List, Movie Detail

ใช้ `innerHTML` เพราะต้องใส่ **HTML template** (มี tag ต่าง ๆ เช่น `<h1>`, `<div>`)  
ถ้าใช้ `textContent` จะแสดงเป็นตัวอักษรธรรมดา ไม่ตีความเป็น HTML

---

### 2. Event listener `click` ที่ใช้ใน movie card ทำหน้าที่อะไร (ดูจาก `onclick`)

ใน movie card มี

```html
onclick="location.hash='#/movie/${movie.id}'"
```

เมื่อคลิก จะเปลี่ยน URL hash
แล้ว Router จะทำงานเพื่อแสดงหน้า Movie Detail

---

### 3. Movie card ใช้ onclick attribute แทน addEventListener ทำไม

เพราะสร้างการ์ดด้วย template string แบบ dynamic
การใส่ onclick ใน HTML ทำได้ง่ายและสั้นกว่า
เหมาะกับงานขนาดเล็กแบบนี้

---

### 4. ฟังก์ชัน `handleSearch()` จะถูกเรียกใช้เมื่อเกิดเหตุการณ์ใด

ถูกเรียกเมื่อ

- พิมพ์ในช่องค้นหา (onkeyup)
- กดปุ่มค้นหา (onclick)

---

### 5. ฟังก์ชัน `handleSearch()` จำเป็นต้องเรียก `Views.loading()` ทำไม

เพื่อแสดงข้อความ "กำลังโหลด..."

ทำให้ผู้ใช้รู้ว่าระบบกำลังดึงข้อมูลจาก API
และป้องกันหน้าว่างระหว่างรอข้อมูล

---

### 6. การแสดงรูปภาพในการ์ด ถ้าไม่มี poster จะแสดงอะไรแทน

จะแสดงรูป placeholder แทน เช่น
https://via.placeholder.com/200x300?text=No+Poster

```id="x7q2mv"

```

### 7. Placeholder URL `via.placeholder.com` ใช้ทำไม

ใช้สำหรับแสดงรูปภาพตัวอย่าง  
ในกรณีที่ไม่มี poster จริง  
เพื่อให้ layout ไม่พังและหน้าตาดูเรียบร้อย

---

### 8. Navigation link ที่มี class `active` จะถูกเลือกด้วย CSS rule ไหน

ใน CSS มี rule แบบนี้

````css
nav a.active {
  background: #149500;
}
``` id="c3mp9k"
````

จะเปลี่ยนสีเมนูที่กำลังใช้งานอยู่

---

### 9. Search results จะแสดงใน element ไหน (ยกตัวอย่าง ID)

จะแสดงใน element ที่มี ID คือ searchResults

โดยใช้

```js
document.getElementById("searchResults");
```

### 10. ฟังก์ชัน `updateNavigation()` จะอัปเดต class ให้ element ไหนบ้าง

จะอัปเดต class ให้ <a> ทุกตัวใน <nav>

โดยเพิ่มหรือลบ class "active"
ตามหน้าปัจจุบันที่กำลังแสดง

---

## ⏳ เรื่อง 6: Async/Await และ Promise

### 1. `async function` คืออะไร และมีข้อดีกว่า `.then()` หรือไม่

async function คือฟังก์ชันที่ คืนค่าเป็น Promise เสมอ โดยอัตโนมัติ

ข้อดีเมื่อเทียบกับ `.then()`

- เขียนโค้ดได้เหมือน synchronous (อ่านง่ายกว่า)
- ลดปัญหา “Promise chain ซ้อนกัน”
- ใช้ `try-catch` จัดการ error ได้ชัดเจนกว่า

---

### 2. Keyword `await` จะทำให้โปรแกรมทำอะไร (halts execution หรือไม่)

`await` จะ หยุดการทำงานของฟังก์ชันนั้นชั่วคราว จนกว่า Promise จะ resolve หรือ reject

หยุดเฉพาะใน async function

---

### 3. ฟังก์ชัน `API.getMovies()` เรียกใช้ `async` ทำไม

เพราะมีการเรียก `fetch()` ซึ่งเป็น asynchronous
จึงต้องใช้ `async` เพื่อให้สามารถใช้ await ได้ และคืนค่า Promise กลับไป

---

### 4. ฟังก์ชัน `navigate()` ใช้ `async` และ `await` อย่างไร

```js
async function navigate(page) {
  Views.loading();

  const movies = await API.getMovies();
  Views.renderMovies(movies);
}
```

### 5. Try-catch block ใน `API.getMovies()` จะจับ error ประเภทไหน

จับ error ที่เกิดจาก:

- network error (เช่น internet หลุด)
- fetch reject
- JSON parse error
- manual throw error

---

### 6. ถ้า `await fetch()` fail จะเกิดอะไร (ให้อธิบาย error handling)

กรณี fail มี 2 แบบ:

- Network error → Promise reject → เข้า catch
- HTTP error (404, 500) → fetch ไม่ reject แต่ res.ok เป็น false
  ต้องเช็คเองแล้ว throw error

ดังนั้น error handling ต้องมี:

- try-catch
- ตรวจ response.ok

---

### 7. Promise chain (`.then().catch()`) ส่วนไหนเหมือน async/await

Promise Chain:`.then()` , `.catch()`
Promise Chain:`await` , `try-catch`

ทั้งสองแบบทำงานเหมือนกัน ต่างกันที่ syntax

---

### 8. Callback function `handleSearch()` ข้างใน `onkeyup` เป็น promise หรือไม่

ไม่จำเป็นต้องเป็น Promise
จะเป็น Promise ก็ต่อเมื่อ:

- ประกาศเป็น async
- หรือ return Promise ออกมา

---

### 9. ฟังก์ชัน `navigate()` await ทั้ง `getMovies()`, `getMovieById()`, `searchMovies()` ทำไม

- รอข้อมูลให้ครบก่อน render
- ควบคุมลำดับการทำงาน
- ป้องกันข้อมูลยังไม่มาแต่หน้าแสดงผลแล้ว

---

### 10. ถ้า API response ช้า user จะเห็นอะไรขณะรอ (ให้อธิบายจาก Views.loading())

จะเห็นผลจาก `Views.loading()` เช่น:

- Loading text
- Spinner
- Skeleton card

เพื่อบอกผู้ใช้ว่าระบบกำลังโหลดข้อมูล

---

## 🛠️ เรื่อง 7: Data Transformation และ Formatting

### 1. OMDB API response ของ search มี fields ไหนบ้าง (ยกตัวอย่าง 3 fields)

- `Title`
- `Year`
- `imdbID`

---

### 2. ฟังก์ชัน `.slice(0, 5)` ใน `getMovies()` ทำอะไร

ใช้เลือกเฉพาะข้อมูล 5 รายการแรกจาก array  
เพื่อจำกัดจำนวนหนังที่แสดงผลในหน้าแรก

---

### 3. เหตุใดจึงต้อง `.map()` OMDB format มาเป็น format ของเรา

ใช้ `.map()` เพื่อ:

- เปลี่ยนชื่อ field ให้สม่ำเสมอ (เช่น `title`, `year`)
- แปลงข้อมูลเป็นชนิดที่ต้องการ
- ควบคุมโครงสร้าง object ให้ตรงกับ UI

---

### 4. Rating ในการ์ด 0 ทำไม แต่ detail page แสดง 8.8 (ให้อธิบายจากข้อมูล OMDB)

หน้า list page:

- ไม่มี rating จริง → จึงตั้งค่า default เป็น `0`

แต่ detail page:

- เรียก API แบบ `i=imdbID`
- ได้ `imdbRating` จริง เช่น `8.8`
- จึงแสดงคะแนนจริงได้

---

### 5. `parseInt(movie.Year)` และ `parseFloat(data.imdbRating)` ทำหน้าที่อะไร

- `parseInt()` → แปลงปีจาก string เป็น number
- `parseFloat()` → แปลงคะแนน IMDb จาก string เป็นเลขทศนิยม

เพื่อให้สามารถคำนวณหรือเปรียบเทียบตัวเลขได้ถูกต้อง

---

### 6. Poster image ที่ "N/A" จะถูกแทนที่ด้วยอะไร

จะถูกแทนที่ด้วย: <https://via.placeholder.com/300x450>
เพื่อแสดงภาพ placeholder แทนภาพจริงที่ไม่มี

---

### 7. Movie object ของเรามี fields ไหนบ้าง (ยกตัวอย่างอย่างน้อย 5 fields)

- `id`
- `title`
- `year`
- `rating`
- `poster`

---

### 8. IMDb ID ที่ใช้คือ imdbID หรือเป็นตัวเลขอื่น

ใช้ค่า `imdbID` จาก OMDB  
เช่น `tt1375666`  
ไม่ใช่ตัวเลขธรรมดา

---

### 9. Genre และ Actors จะปรากฏที่หน้าไหนในแอป (detail page หรือ list page)

จะแสดงใน **Detail Page**  
เพราะข้อมูลเหล่านี้มีเฉพาะใน API แบบรายละเอียด (ไม่ใช่ search)

---

### 10. Plot ใน API response จะแสดงใน UI ที่ไหน (ให้อธิบายตำแหน่ง)

จะแสดงในหน้า **Detail Page**

ตำแหน่งโดยทั่วไป:

- ใต้ชื่อหนัง
- ใต้ข้อมูลปีและ rating
- อยู่ในส่วนคำอธิบายเนื้อเรื่อง (description section)

---

## ✅ เรื่อง 8: Testing และ Debugging

### 1. Console.log ที่ใช้ใน main.js แสดงข้อมูลอะไร

- ข้อมูลที่ได้จาก API (เช่น movie array)
- ค่า route ที่กำลัง navigate
- ค่าที่ได้จาก input search
- error ที่เกิดขึ้น

---

### 2. ถ้า movie ไม่โหลด ควรตรวจสอบจาก browser tools ที่ไหนอย่างไร

ให้เปิด **DevTools (F12)** แล้วตรวจสอบ:

1. **Console tab**
   - ดู error สีแดง
   - ดู log ว่าข้อมูลเป็น undefined หรือไม่

2. **Network tab**
   - ดูว่ามี request ไป OMDB หรือไม่
   - ตรวจสอบ status code

---

### 3. Network tab จะแสดง API request ที่ fail หรือ success ได้ยังไง

- **Status Code**
  - `200` = สำเร็จ
  - `404/401/500` = error

- สีของ request
  - ปกติ = success
  - สีแดง = fail

- คลิกเข้าไปดูในแท็บ **Response**

---

### 4. Console error "API Error: ..." มาจากการตรวจสอบอะไร

```js
if (!response.ok) {
  throw new Error("API Error");
}
```

คือเป็น error ที่เรา throw เองจากการตรวจสอบ response

---

### 5. ถ้า API key ผิด OMDB จะส่ง Response ที่เป็น "False" คืออะไร

```json
{
  "Response": "False",
  "Error": "Invalid API key!"
}
```

- request สำเร็จในระดับ HTTP
- แต่ logic ภายในผิด (เช่น API key ไม่ถูกต้อง)

---

### 6. `handleSearch()` ควรทดสอบด้วยการค้นหาคำไหนในแอป

ควรทดสอบหลายกรณี เช่น:

- "batman" (มีผลลัพธ์เยอะ)
- "avengers"
- คำที่ไม่มีจริง เช่น "zzzz12345"
- ช่องว่างหรือ input ว่าง
  เพื่อทดสอบทั้ง success และ no result

---

### 7. Browser DevTools ส่วนไหนสำคัญสำหรับ debug async/await

- Console tab → ดู error จาก try-catch
- Network tab → ดู request/response timing
- Sources tab → ใช้ breakpoint ดู flow ของ await

---

### 8. ตรวจสอบความเร็ว API response (cache หรือ fetch) ทำไง

ใน Network tab:

1. ดูคอลัมน์ Time
2. ดูค่า Waterfall
3. คลิก request แล้วดู: Timing ,From memory cache หรือ From disk cache

---

### 9. การค้นหา "movie" ควรคืนภาพยนตร์กี่เรื่อง (ยกตัวอย่างสอง-สามเรื่อง)

โดยปกติ OMDB จะคืนหลายรายการ (เช่น 10 รายการต่อหน้า)

ตัวอย่างผลลัพธ์อาจมี:

- Movie 43
- The Lego Movie
- Scary Movie

จำนวนจริงขึ้นกับ page และ API response

---

### 10. ถ้า search ไม่แสดงผลลัพธ์ ให้เช็ค API response ใน console ด้วยคำสั่งไหน

```js
console.log(await response.json());
```

เพื่อดูว่า:

- Search มีข้อมูลหรือไม่
- Response เป็น "True" หรือ "False"

---

## 📱 เรื่อง 9: CSS และ UI/UX

### 1. Movie card layout ใช้ CSS ตัวไหน (Grid หรือ Flexbox)

ใช้ **CSS Grid**  
เพราะมีการกำหนด `grid-template-columns` สำหรับจัดเรียงการ์ดหลายคอลัมน์

---

### 2. `grid-template-columns: repeat(auto-fill, minmax(300px, 1fr))` ทำอะไร

- `repeat(auto-fill, ...)` → สร้างคอลัมน์อัตโนมัติตามพื้นที่หน้าจอ
- `minmax(300px, 1fr)` → แต่ละคอลัมน์:
  - กว้างขั้นต่ำ 300px
  - ขยายได้สูงสุด 1 ส่วน (1fr)

การ์ดจะ responsive ตามขนาดหน้าจออัตโนมัติ

---

### 3. Transition `transform 0.3s` ใน `.movie-card:hover` ทำอะไร

ทำให้การเปลี่ยนแปลง (เช่น `scale` หรือ `translateY`)  
เคลื่อนไหวอย่างนุ่มนวลภายใน 0.3 วินาที

ช่วยให้ UX ดูลื่นไหล ไม่กระตุก

---

### 4. Placeholder image URL ใช้เพื่อจุดประสงค์ใด

ใช้แทนภาพ poster ที่ไม่มี (`N/A`)  
เพื่อ:

- ป้องกัน layout พัง
- รักษาขนาดการ์ดให้เท่ากัน
- ทำให้ UI ดูสมบูรณ์

---

### 5. Navigation link ที่ `active` จะมี background color มีสีไหน

โดยทั่วไปจะใช้:

- สีเข้มกว่า link ปกติ
- หรือสี highlight เช่น น้ำเงิน / ม่วง / เทาเข้ม

(ขึ้นกับธีมที่กำหนดใน CSS ของแอป)

---

### 6. Movie detail page แสดงภาพและข้อมูลในรูปแบบไหน (side-by-side หรือ top-bottom)

แสดงแบบ **side-by-side** (ภาพอยู่ซ้าย ข้อมูลอยู่ขวา)

แต่ถ้าหน้าจอแคบ (มือถือ):

- จะเปลี่ยนเป็น top-bottom (ซ้อนกัน)

---

### 7. Loading state จะแสดง emoji อะไร และนี่สำคัญทำไม

มักใช้ emoji เช่น:

- ⏳
- 🎬
- 🔄

ความสำคัญ:

- บอกผู้ใช้ว่าระบบกำลังโหลด
- ป้องกันความสับสนว่าแอปค้าง
- ช่วยให้ UX ดีขึ้น

---

### 8. `box-shadow` ที่ใช้ใน movie detail poster ทำอะไร

เพิ่มเงาให้รูปภาพ

ผลลัพธ์:

- ทำให้ภาพดูมีมิติ
- ดูลอยจากพื้นหลัง
- เพิ่มความสวยงาม

---

### 9. Responsive design: ถ้า screen แคบ movie grid จะมีการ์ดกี่คอลัมน์

เมื่อหน้าจอแคบมาก:

- เหลือ 1 คอลัมน์
  หรือ
- 2 คอลัมน์ (ถ้ายังมีพื้นที่มากกว่า 300px ต่อการ์ด)

ขึ้นกับความกว้างหน้าจอ

---

### 10. Footer ของแอปจะแสดงองค์ประกอบไหนบ้าง

โดยทั่วไปมี:

- ชื่อแอป
- เครดิตผู้พัฒนา
- © Copyright
- ปีปัจจุบัน

บางแอปอาจมี:

- ลิงก์ GitHub
- ข้อมูล API ที่ใช้

---

## 🔐 เรื่อง 10: Security และ Best Practices

### 1. API key ไว้ใน client-side code มีความเสี่ยงหรือไม่ เหตุใด

มีความเสี่ยง
เพราะ:

- ใครก็สามารถเปิด DevTools แล้วดู source code ได้
- สามารถ copy API key ไปใช้ที่อื่นได้
- อาจทำให้ quota หมด หรือถูกนำไปใช้ในทางที่ผิด

---

### 2. วิธีแก้ไข security issue ของ API key คืออะไร

แนวทางที่ถูกต้อง:

- เก็บ API key ไว้ใน **backend server**
- ใช้ environment variables (เช่น `.env`)
- ให้ frontend เรียก backend แทนการเรียก OMDB ตรง ๆ

โครงสร้างที่ปลอดภัย: Frontend → Backend → OMDB API

---

### 3. OMDB API มีการจำกัด rate limit หรือไม่ (ฟรี account ได้กี่ requests)

มีการจำกัด

บัญชีฟรีของ OMDB:

- ประมาณ **1,000 requests ต่อวัน**

หากเกิน limit:

- API จะตอบกลับ error

---

### 4. Error handling ควรมี try-catch หรือเพียง `.catch()` ก็พอ

ขึ้นกับรูปแบบที่ใช้:

- ใช้ async/await → ควรใช้ `try-catch`
- ใช้ Promise chain → ใช้ `.catch()`

Best practice:

- ควรมีการจัดการ error เสมอ
- และควร handle ทั้ง network error และ logic error (`Response: "False"`)

---

### 5. Input validation ของการค้นหา ควรตรวจสอบอะไรบ้าง

ควรตรวจสอบ:

- ค่าว่าง (`""`)
- ช่องว่างล้วน (`"   "`)
- ตัวอักษรยาวเกินไป
- อักขระพิเศษผิดปกติ

ควรใช้:

```js
if (!query.trim()) return;
```

### 6. Script tag order ใน HTML มีความสำคัญ (api.js ก่อน main.js) ทำไม

สำคัญมาก

ถ้า `main.js` ใช้ฟังก์ชันจาก `api.js`
ต้องโหลด `api.js` ก่อน

เพราะ:

- JavaScript อ่านจากบนลงล่าง
- ถ้าเรียกก่อนโหลด → จะเกิด undefined error

---

### 7. `console.error()` ใช้ในไหนบ้าง และควรแสดงให้ user เห็นหรือไม่

ใช้ใน:

- catch block
- debug error ภายในระบบ

ไม่ควรแสดง error ดิบ ๆ ให้ user เห็น ควรแสดงข้อความที่เป็นมิตร

---

### 8. Sensitive data (API key) ไม่ควรโปรแกรมใน source code แต่ดึงจากไหน

ควรดึงจาก:

- Environment variables (process.env.API_KEY)
- ไฟล์ .env ใน backend
- Server configuration

ไม่ควร hardcode ใน frontend

---

### 9. HTTPS จำเป็นสำหรับคำขอ OMDB API หรือไม่

จำเป็น

เหตุผล:

- เข้ารหัสข้อมูล
- ป้องกันการดักจับ API key
- Browser สมัยใหม่บังคับใช้ HTTPS

OMDB ใช้ HTTPS อยู่แล้ว

---

### 10. Browser security policy จะบล็อก request ไปยัง HTTP endpoint หรือไม่ (OMDB uses HTTPS)

ถ้าเว็บไซต์โหลดผ่าน HTTPS
แล้วเรียก HTTP (ไม่เข้ารหัส) → จะถูกบล็อก (Mixed Content)

แต่ OMDB ใช้ HTTPS
จึงไม่ถูกบล็อกตาม browser security policy

---

# ❓ คำถามวัดความเข้าใจ - Lab10: Movie Database SPA

---

## 📚 เรื่อง 1: Fetch API และ HTTP Requests

### 1. ฟังก์ชัน `fetch()` ใช้ส่ง HTTP request ประเภทไหนเป็นค่าเริ่มต้น

ค่าเริ่มต้นคือ **GET request**  
ถ้าไม่กำหนด method เอง ระบบจะใช้ GET อัตโนมัติ

---

### 2. เหตุใดจึงต้องใช้ `.json()` หลังจาก `fetch()`

เพราะ `fetch()` จะได้ค่าเป็น Response object ยังไม่ใช่ข้อมูลจริง  
ต้องใช้ `.json()` เพื่อแปลงข้อมูล JSON ให้เป็น JavaScript object ก่อนจึงจะนำไปใช้งานได้

---

### 3. API response ของ OMDB มี Error handling อย่างไร (ยกตัวอย่างจากโค้ด)

OMDB จะส่งค่าแบบนี้เมื่อเกิดข้อผิดพลาด

```json
{
  "Response": "False",
  "Error": "Movie not found!"
}
```

```js
if (data.Response === "False") {
  console.error("API Error:", data.Error);
  return [];
}
```

ถ้า Response เป็น False จะถือว่าเกิด error แล้ว return ค่าว่างกลับไป

### 4. ถ้า API key ผิด จะได้ข้อมูลเป็นอะไร และควรตรวจสอบอย่างไร

```json
{
  "Response": "False",
  "Error": "Invalid API key!"
}
```

ควรตรวจสอบโดยเช็คว่า data.Response === "False" แล้วแสดง error ออกมา

---

### 5. ฟังก์ชัน `API.getMovies()` ดึงข้อมูลภาพยนตร์กี่เรื่อง และค้นหาคำว่าอะไร

ฟังก์ชันนี้ค้นหาคำว่า "movie"
และใช้ .slice(0, 5)

- ดังนั้นจะดึงมา 5 เรื่องแรก

---

### 6. ความแตกต่างระหว่าง `API.getMovies()` กับ `API.searchMovies()` คืออะไร

- getMovies() ค้นหาคำว่า "movie" และจำกัด 5 เรื่อง

- searchMovies(query) ค้นหาตามคำที่ผู้ใช้พิมพ์ และไม่จำกัดแค่ 5 เรื่อง

---

### 7. ฟังก์ชัน `API.getMovieById()` ใช้พารามิเตอร์อะไรในการค้นหาภาพยนตร์เรื่องเดี่ยว

ใช้ IMDb ID เช่น tt0133093
โดยส่งค่าแบบ ?i=imdbID

---

### 8. ทำไม OMDB search response ไม่มี rating แต่ detail response มี

เพราะ API แยกเป็น 2 แบบ

- ?s= ใช้ค้นหา จะได้ข้อมูลแบบย่อ (ไม่มี rating)

- ?i= ใช้ดูรายละเอียด จะได้ข้อมูลครบ รวมถึง rating

---

### 9. URL ของ OMDB API ในโค้ดชี้ไปยังเซิร์ฟเวอร์ไหน (ให้ดูจาก `BASE_URL`)

ชี้ไปที่ <https://www.omdbapi.com> ซึ่งเป็นเซิร์ฟเวอร์ของ OMDb API

### 10. ฟังก์ชัน `API.searchMovies()` จะส่งอะไรกลับมา ถ้า `query` เป็นว่างหรือ null

ถ้า query ว่าง จะส่งกลับมาเป็น array ว่าง ([])

---
